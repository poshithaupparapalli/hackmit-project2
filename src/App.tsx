/**
 * Frontend entry point.
 *
 * MiaDemo is intentionally NOT mounted here yet — import it from
 * `./components/mia/MiaDemo` once the team's real endpoints
 * (Kathy's /suggestions) are ready to wire in.
 */
export function App() {
  return <main style={{ fontFamily: 'system-ui, sans-serif', padding: 24 }}>MIA frontend — no routes wired up yet.</main>;
}
