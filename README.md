# Mia

The Chrome extension is in [`extension/`](extension/). See its [setup and demo guide](extension/README.md) to load it unpacked, rehearse the receipt workflow, and connect Kathy's backend.

[`MIA_CONTRACTS.md`](MIA_CONTRACTS.md) is the frozen v1.1 integration contract. Backend detection, OAuth, workflow execution, and the web frontend are owned separately.
